# fixture only; canonical CRD ships with the operator project
# (github.com/donaldgifford/wiz-operator). Same role as project.yaml
# — Phase 2 doesn't write UserRole CRs, the schema is the minimum
# envtest needs to load the type.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: userroles.wiz.webhookd.io
spec:
  group: wiz.webhookd.io
  names:
    kind: UserRole
    listKind: UserRoleList
    plural: userroles
    singular: userrole
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              properties:
                name:
                  type: string
            status:
              type: object
              properties:
                observedGeneration:
                  type: integer
                  format: int64
                conditions:
                  type: array
                  items:
                    type: object
                    required: [type, status, lastTransitionTime, reason, message]
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      observedGeneration:
                        type: integer
                        format: int64
                      lastTransitionTime:
                        type: string
                        format: date-time
                      reason:
                        type: string
                      message:
                        type: string
      subresources:
        status: {}
